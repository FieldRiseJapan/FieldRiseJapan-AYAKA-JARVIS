# Differential PNG asset contract (awaiting approved artwork)

- Use separate RGBA PNG files with transparent backgrounds under `assets/characters/ayaka/animation/eyes/` (`eyes_open.png`, `eyes_half.png`, `eyes_closed.png`) and `animation/mouth/` (`mouth_closed.png`, `mouth_small.png`, `mouth_medium.png`, `mouth_open.png`). These are proposed names; register only after approved artwork exists.
- Each layer must use the same native canvas dimensions and pixel origin as `ayaka_left_official.png`; no estimated facial anchor is allowed. Canvas anchor is `(0, 0)` in source image coordinates, with X/Y alignment recorded from the source artwork or author-provided coordinates. Preserve transparent pixels and straight alpha, and composite over the official image before scaling to the LEFT work area. Eye layer precedes mouth layer; HUD remains on top.
- Provide full native resolution, exact measured X/Y registration and approved layer order with the assets. Confirm that open/closed artwork covers the intended source region without altering the official source file. Do not fabricate a mask or infer feature positions from the single PNG.
- When any required file is absent, invalid, misaligned or cannot load, disable that visual feature and retain the unchanged static image. Confirm registered artwork by visual review on the actual Windows display before enabling a differential provider.
